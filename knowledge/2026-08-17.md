# Daily Progress — 2026-08-17

## Branch
main

## Files Changed
- `src/components/AudioGuide.tsx` — refactored from fixed-position floating button to inline header button
- `src/components/DefaultLayout.tsx` — removed AudioGuide (moved to Header)
- `src/components/Header.tsx` — added AudioGuide as rightmost element in nav (desktop + mobile)
- `src/content/home/contact.md` — updated email address

## Summary
Session focused on three main tasks: removing certificates carousel from hero section, creating a dedicated certificates page with skill-based filtering, and moving the audio guide button into the header.

## Details

### 1. Certificates Page (committed earlier in session)
- Removed `CertificatesCarousel` from `HeroSection.tsx`
- Created new standalone `/certificates` page (`src/pages/Certificates.tsx`)
- Added `/certificates` and `/certificates/:skill` routes in `App.tsx`
- Restructured `certificates.json` with `skills[]` array (slug, name, level override)
- Updated `lib/certs.ts` with new helpers: `getSkills()`, `getCertificatesBySkill()`, `getSkillLevel()`
- Updated `SkillsSection.tsx` to load skills from `certs.ts`, cards now clickable → `/certificates/:slug`
- Skill percentage logic: 20% base + 10% per certificate, manual override via `level` field
- Deleted unused `CertificatesCarousel.tsx`
- Moved PDFs into subfolders: `public/certificates/anthropic/`, `public/certificates/git-github/`

### 2. Audio Guide → Header (uncommitted)
- Refactored `AudioGuide.tsx` — removed fixed positioning (`fixed bottom-6 right-6`), simplified to inline button with speaker + play/pause icons
- Removed `showControls` prop (no longer needed)
- Removed `<AudioGuide>` from `DefaultLayout.tsx`
- Added `<AudioGuide>` to `Header.tsx` — rightmost position after Achievements dropdown (desktop) and before hamburger menu (mobile)

### 3. Contact Email Update (uncommitted)
- Updated `contact.md` email from `hello@mrant.dev` to `anthtooaung2792005@gmail.com`
