---
phase: 1
plan: 01
type: refactor
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/HeroSection.tsx
autonomous: true
requirements:
  - THME-01
  - THME-02
  - THME-03
  - THME-04
---

# Plan 01: Warm Theme Foundation

<objective>
Update design tokens to a cohesive warm amber/gold palette across light and dark modes, and refactor the hero gradient utility to use the primary CSS variable.
</objective>

## Tasks

```xml
<task>
  <id>01-01-01</id>
  <title>Update Light and Dark Mode Tokens</title>
  <type>refactor</type>
  <read_first>
    - src/index.css
    - .planning/phases/01-warm-theme-route-skeleton/01-UI-SPEC.md
  </read_first>
  <action>
    In `src/index.css`, update the `:root` and `.dark` blocks with the exact OKLCH values specified in `01-UI-SPEC.md`. This shifts the palette from hue 280 (violet) to hue 75 (warm amber/cream).
  </action>
  <verify>Check that both `:root` and `.dark` contain the new OKLCH values.</verify>
  <acceptance_criteria>
    - src/index.css `:root` block contains `--background: oklch(0.99 0.005 75);`
    - src/index.css `.dark` block contains `--background: oklch(0.15 0.01 75);`
  </acceptance_criteria>
</task>

<task>
  <id>01-01-02</id>
  <title>Refactor Hero Gradient</title>
  <type>refactor</type>
  <read_first>
    - src/index.css
    - src/components/HeroSection.tsx
  </read_first>
  <action>
    In `src/index.css`, rename `.text-gradient-violet` to `.text-gradient-warm`. Update the `background` property to:
    `background: linear-gradient(135deg, var(--primary), oklch(0.78 0.12 75));`
    
    In `src/components/HeroSection.tsx`, update `<span className="text-gradient-violet">{title}</span>` to use `text-gradient-warm`.
  </action>
  <verify>Build runs successfully.</verify>
  <acceptance_criteria>
    - src/index.css contains `.text-gradient-warm` with `linear-gradient(135deg, var(--primary), oklch(0.78 0.12 75))`
    - src/components/HeroSection.tsx contains `className="text-gradient-warm"`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>
```

## Artifacts this phase produces
- `.text-gradient-warm` utility class

## Verification
1. Run `npm run build` to ensure valid CSS and JSX.
2. Manually verify WCAG AA contrast (4.5:1) for text elements on the new background colors.
3. Toggle dark mode in the browser to confirm smooth transition without jarring hue flash.

## must_haves
- truths:
  - "Site displays warm cream/amber tones in light mode and complementary warm dark tones in dark mode"
  - "Toggling between light and dark mode produces no jarring hue flash"
  - "Hero title gradient title wraps at narrow viewports; gradient applies per-line via background-clip: text, so wrapped text shows gradient correctly on each line."
  - "Dark mode toggle label is a Phosphor icon (Sun/Moon) — no text length concern."
