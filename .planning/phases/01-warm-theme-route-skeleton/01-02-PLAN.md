---
phase: 1
plan: 02
type: feat
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/Header.tsx
  - src/components/DefaultLayout.tsx
  - src/components/ScrollToTop.tsx
  - src/pages/Resume.tsx
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
---

# Plan 02: Route Skeleton and Conditional Chrome

<objective>
Configure react-router-dom to support a `/resume` page with Header/Footer hidden, update the header navigation to Link to `/resume`, scroll to top on routing, and verify no scroll-spy errors.
</objective>

## Tasks

```xml
<task>
  <id>01-02-01</id>
  <title>Create DefaultLayout and ScrollToTop Components</title>
  <type>feat</type>
  <read_first>
    - src/App.tsx
  </read_first>
  <action>
    Create `src/components/DefaultLayout.tsx` which imports `Header`, `Footer`, and `Outlet` from `react-router-dom`. It should render `Header`, then `main` wrapping `Outlet` (flex-grow), then `Footer`. Use named export: `export function DefaultLayout()`.
    
    Create `src/components/ScrollToTop.tsx` which imports `useEffect` and `useLocation` from `react-router-dom`. It runs `window.scrollTo(0, 0)` on route change. Use named export: `export function ScrollToTop()`.
  </action>
  <verify>Build runs successfully.</verify>
  <acceptance_criteria>
    - `src/components/DefaultLayout.tsx` exists and renders Header, Outlet, and Footer
    - `src/components/ScrollToTop.tsx` exists and calls window.scrollTo(0, 0) in useEffect on location change
  </acceptance_criteria>
</task>

<task>
  <id>01-02-02</id>
  <title>Create Resume Page Placeholder</title>
  <type>feat</type>
  <read_first>
    - src/pages/Home.tsx
  </read_first>
  <action>
    Create `src/pages/Resume.tsx`. Render a simple placeholder: a container div, a heading `h1` saying "Resume", and a home link or similar. Ensure it uses named export: `export function ResumePage()`.
  </action>
  <verify>File exists and compiles.</verify>
  <acceptance_criteria>
    - `src/pages/Resume.tsx` exists and exports `ResumePage`
  </acceptance_criteria>
</task>

<task>
  <id>01-02-03</id>
  <title>Configure Routes in App.tsx</title>
  <type>feat</type>
  <read_first>
    - src/App.tsx
    - src/components/DefaultLayout.tsx
    - src/components/ScrollToTop.tsx
    - src/pages/Resume.tsx
  </read_first>
  <action>
    Modify `src/App.tsx`. Wire up `ScrollToTop` inside `BrowserRouter` but outside `Routes`. Refactor the route tree:
    - Root route `/` wraps inside `DefaultLayout` as layout route, rendering `Home`.
    - Route `/resume` renders `ResumePage` standalone (outside `DefaultLayout`).
  </action>
  <verify>App.tsx compiles and runs.</verify>
  <acceptance_criteria>
    - `src/App.tsx` has `ScrollToTop` component wired
    - `src/App.tsx` configures layout routing via `<Route element={<DefaultLayout />}>`
    - `/resume` route is a sibling to the layout route and renders `ResumePage` directly
  </acceptance_criteria>
</task>

<task>
  <id>01-02-04</id>
  <title>Add Resume Link to Header</title>
  <type>feat</type>
  <read_first>
    - src/components/Header.tsx
  </read_first>
  <action>
    In `src/components/Header.tsx`, import `Link` from `react-router-dom` and the `FileText` icon.
    Update the Header nav component:
    - The Resume link should go after "Contact". It is styled the same as other nav items.
    - It must use `Link` to `/resume` instead of hash scroll anchors.
    - Ensure the Resume link is added to both the desktop nav and the mobile menu.
    - The active state sliding indicator should ignore the Resume link (or do not include it in indicators). Do not highlight it on scroll spy.
  </action>
  <verify>Header links rendering matches intent.</verify>
  <acceptance_criteria>
    - `Header.tsx` imports `Link` from `react-router-dom`
    - Header nav contains Resume link after Contact that navigates to `/resume`
    - Mobile menu in Header contains Resume link after Contact
    - Resume link does not trigger scroll-spy or active sliding indicator
  </acceptance_criteria>
</task>
```

## Artifacts this phase produces
- `DefaultLayout` (layout wrapper component)
- `ScrollToTop` (scroll utility component)
- `ResumePage` (pages component)

## Verification
1. Navigate to `/resume` and verify Header/Footer are hidden.
2. Confirm clicking the Resume link in Header navigates to `/resume`.
3. Confirm scroll spy hook does not throw any errors on `/resume` (since Header/Footer do not mount, scroll spy hook doesn't run).
4. Verify navigating from a scrolled position on home `/` to `/resume` resets scroll to (0, 0).

## must_haves
- truths:
  - "Clicking 'Resume' in the header navigates to `/resume` with Header and Footer hidden"
  - "Navigating to `/resume` from `/` scrolls to the top of the resume page"
  - "Scroll-spy navigation does not activate or throw errors on the `/resume` page"
  - "Resume link styled same as other nav items"
  - "Resume link appears in the mobile menu too"
  - "Resume link position: after Contact in the nav order"
  - "Resume link is never scroll-spy highlighted"
  - "Layout container — no data or interactive state."
  - "Static nav link — not a data-driven element; no state surface."
  - "Utility component — fires on mount, no visual state."
